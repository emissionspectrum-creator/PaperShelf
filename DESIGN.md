# 考題呈現平台 — DESIGN.md

本文件為跨 session 的正式規格，是實作的唯一依據。若實作過程中發現規格有問題，先修改本文件，再改程式。

---

## 1. 專案目標

提供一個極簡的方式，讓小孩在平板或電腦螢幕上閱讀考題，並在另外的紙上作答。

本專案**不負責**出題、審題、批改、成績記錄。

### 前一版失敗的原因

前一版（ExamGen）將「分析考題／出題」與「呈現考題」寫在同一個程式裡，導致系統龐大複雜、難以維護。本專案是重新設計的替代品。

---

## 2. 設計原則

依優先順序：

1. **凡是需要判斷力的工作，都移出程式**——出題、審題、校正、批改，全部由人或 Claude 對話處理
2. **能交給既有工具的，不自己實作**——縮放交給瀏覽器，版本控制交給 git，發布交給 GitHub Pages
3. **無後端、無資料庫、無狀態**
4. 小到不可能壞掉。功能的增加需要明確理由

---

## 3. 系統邊界

### 明確不做的事

- 不出題、不分析考題
- 不做影像處理（裁切、旋轉由人在上游完成）
- 不儲存答案、不顯示答案
- 不記錄作答、不記錄對錯、不累積錯題
- 不做題庫管理、不支援搜尋題目
- 不自己實作縮放功能

### 元件

| 元件 | 位置 | 職責 |
|---|---|---|
| 打包器 | 本地 | 挑選、排序、逐題縮放校正、輸出考卷 HTML 與 index |
| 考卷 HTML | GitHub Pages | 依序顯示題目圖像 |
| index.html | GitHub Pages | 列出所有考卷、分類、導向 |

責任界線：**打包器負責「讓每一題長得對」，考卷 HTML 負責什麼都不做。**

### 專案資料夾結構

```
PaperShelf/
  DESIGN.md
  packager/          打包器程式碼（本機伺服器 + 前端頁面）
  source-images/      原始題目圖像（打包前素材，已裁切旋轉完成）
  manifest.json        清單檔（見第 9 節）
  docs/                GitHub Pages 實際發布的內容
    index.html
    exams/*.html
```

GitHub Pages 設定為只發布 `/docs`，`packager/`、`source-images/`、`manifest.json` 雖進 git 版控（repo 私有），但不會出現在公開站台上。

---

## 4. 資料流

```
Claude Project 出題 → 內容確認（此階段必須完成正確性檢查）
      ↓
產出題目圖像（Claude 產 HTML → 瀏覽器截圖 / 手動掃描既有考卷）
      ↓
打包器：挑選 → 排序 → 輸入分類資訊 → 預覽 → 逐題縮放校正
      ↓
輸出：考卷 HTML（單檔、自給自足）+ 重新產生的 index.html + 更新清單檔
      ↓
打包器內一鍵 git commit / push → GitHub Pages 更新
      ↓
平板 / 電腦開網址閱讀
```

**關鍵約束**：圖像一旦產出即無法修改。題目內容的正確性必須在產圖之前確認完畢，發現錯誤只能重產。

---

## 5. 題目圖像規格（上游規格，平台無法補救）

- **一張圖 = 一題**
- 兩種來源：Claude 生成、手動掃描或截圖既有考卷
- 裁切與旋轉在進入打包器之前完成
- **解析度需約 2 倍於實際顯示所需尺寸**。圖像為點陣，放大超過原始解析度會糊；使用者會用瀏覽器縮放放大閱讀
- 建議做法：截圖時提高瀏覽器縮放比例，或使用高 DPI 截圖

### 已驗證的可行性

Claude 產出的注音（`ruby` + `ruby-position: inter-character`）與台灣格式直式計算，品質已確認達到要求。

**建議**：將驗證用的 HTML 存為範本。往後出題時給 Claude 範本填內容，品質較穩定，不需每次重新交代排版規格。

---

## 6. 打包器

### 輸入

- 一批題目圖像（已裁切、已排序或待排序）
- 分類資訊（打包時明確輸入，欄位定義見第 9 節）

### 處理

1. **統一圖框寬度**——CSS 容器寬度一致，不改動圖片本身像素尺寸（屬於顯示層調整，不是第 3 節禁止的影像處理）
2. **預覽**
3. **逐題縮放校正**——人工目視調整個別題目的縮放比例；此比例是 CSS 層級的數值（套用在容器內圖片的寬度或 `transform: scale()`），寫入輸出，不重新編碼圖片像素

> 為何需要第 3 步：統一圖框寬度只保證容器一致，不保證內容字級一致。掃描件的留白比例與生成圖不同，裁成同寬後字級看起來仍會差很多。判斷「這一題的字太小」需要影像分析，複雜度過高——交給眼睛。

### 預覽的必要條件

- **必須直接載入產好的那份考卷 HTML**，不可另寫一套顯示邏輯。若預覽與最終產物不同，此檢查步驟即失去意義。預覽即第 7 節所定義的依序、直式捲動呈現，不另做並排比較用的 grid 檢視

### 輸出

1. 考卷 HTML（單檔，見第 7 節）
2. 重新產生的 index.html（見第 8 節）
3. 更新後的清單檔（見第 9 節）
4. 可選：一鍵執行 `git add` + `commit` + `push`，將上述輸出發布到 GitHub Pages

### 執行方式

**本地網頁 + 極簡本機伺服器**：瀏覽器開啟 `localhost`，實際的圖片讀取、檔案寫入、git 指令執行都由本機伺服器處理，不依賴瀏覽器的 File System Access API（該 API 僅 Chromium 系支援，且在 `file://` 協定下的行為未受驗證，不作為依賴）。此伺服器僅在本機執行，不對外開放，屬於第 2 節「無後端」原則規範的對象是第 7、8 節的發布站台，不含本機打包工具。

---

## 7. 考卷 HTML 規格

### 自給自足

每份考卷是**單一個 HTML 檔**，所有圖像以 base64 內嵌。

理由：Android 上的雲端儲存 app 會將下載的檔案放在自己的沙箱中，瀏覽器無法以相對路徑讀取隔壁的圖檔。單檔可避開所有路徑與資料夾結構問題，且可離線開啟。

代價：base64 編碼使體積膨脹約 33%。此量級可接受。

### 顯示

- 依序顯示題目圖像
- **白底**。不使用純白 `#FFFFFF`；圖像周圍使用接近紙色的極淺灰白，使白底的題目圖具有淡邊界

> 為何不用暗色底：題目圖本身即為白底黑字，無法變暗。暗色介面會造成「暗背景中浮著亮白紙張」的光暈，對比比純白背景更刺眼。且孩子視線在螢幕與白紙之間往返，兩者明暗一致可避免反覆的明暗適應疲勞。

### 必須遵守

- **相對寬度，不得寫死像素寬度**。否則瀏覽器縮放時內容會超出畫面，需左右捲動
- **不得停用使用者縮放**（不宣告 `user-scalable=no`）
- 檔名與頁面標題不得包含孩子姓名或學校名稱

### 沒有的功能

不實作任何縮放控制。縮放交給瀏覽器原生功能（捏合手勢、Ctrl + 滾輪），瀏覽器亦會自動記住各站台的縮放設定。

---

## 8. index.html 規格

- 由打包器產生，**不手寫**（避免每次新增考卷都要手動維護清單所累積的摩擦與錯誤）
- 可依年級、科目篩選瀏覽
- **排序：依加入清單的時間，新的在上**（非依考卷本身的範圍或日期）
- 分類資訊來自清單檔

### 隱私

index 是唯一會被完整瀏覽的公開頁面。**標題不得包含孩子姓名或學校名稱。**

---

## 9. 清單檔

打包器維護一份本地清單檔，記錄所有考卷及其分類資訊。

- 這是本專案中唯一的持久狀態，僅存在本地
- index 產生時讀取此檔
- **納入 git 版控**。遺失則需重建全部分類資訊
- 不進考卷 HTML、不影響閱讀端
- 打包器須提供管理介面：可對清單**新增、刪除**項目，不只是附加寫入

### 欄位

| 欄位 | 說明 |
|---|---|
| 年級 | 下拉選單：國小一年級 ～ 六年級 |
| 科目 | 下拉選單：國文、數學 |
| 序號 | 同一年級＋科目底下的流水號，構成識別碼「年級-科目-序號」 |
| 範圍 | 自由文字，考試範圍/章節說明（例如「第三課」），供日後尋找考卷用 |

加入清單的時間由打包器自動記錄，供第 8 節 index 排序使用，不需人工輸入。

存在理由：分類資訊為打包時明確輸入，若不保存，每次重新產生 index 都需重新輸入全部考卷的資訊。

---

## 10. 部署

**GitHub Pages。** 發布方式即 `git push`，順便獲得版本控制。

### 已知的取捨

- **repo 為公開，不是私有。** 原先設想「repo 私有、站台公開」需要 GitHub Pro / Team 以上方案；實際嘗試時發現目前帳號的方案（Free）連這個折衷都不提供——私有 repo 直接無法開啟 Pages（API 回傳 422 `Your current plan does not support GitHub Pages for this repository`）。已與使用者確認，選擇改用公開 repo，而非升級付費方案
- 這代表 `source-images/`（原始題目圖片）與 `packager/`（打包器程式碼）現在也是公開的，可被任何人透過 GitHub 直接瀏覽，不只是 Pages 站台，暴露範圍比原設想更大
- 以「網址/repo 不外流」作為現實層面的保護
- 因此第 7、8 節的姓名與學校限制是必要的，不是可選的；第 12 節「著作權」提到的掃描既有考卷風險，因 repo 公開而更需留意

### 已知限制

- **倉庫會持續累積**。base64 內嵌加上 git 保留歷史，長期下來體積會顯著成長。若某日過肥，正確做法是開一個新 repo 重新開始，而非嘗試清理 git 歷史
- GitHub Pages 有 1GB 站台大小上限。屬「值得知道」而非「需要擔心」的等級，但因圖像內嵌，比一般靜態站台更接近此線

### 曾評估但未採用

| 方案 | 優點 | 未採用的原因 |
|---|---|---|
| Cloudflare Pages | Access 提供免費存取控制，站台可真正私有 | 需多學一個平台；Cloudflare 已建議新專案改用 Workers，Pages 走向維護模式 |
| Tailscale 自架 | 完全私密，不上雲 | 電腦須持續開機 |

若日後決定必須保護站台內容，建議走 Tailscale（已在使用，不需新增平台），而非 Cloudflare。

---

## 11. 答案的處理

**答案不進本系統。**

- 不產出第二份 HTML
- 不實作答案遮蔽／顯示
- 由人看孩子紙上的作答並批改

**但答案不是「不存在」。** Claude Project 出題時會產出答案，該內容留在 Project 對話紀錄或本地檔案中。批改時自行參照。

⚠️ 出題流程需確保答案有被保留下來。

---

## 12. 未決事項

### 風格一致性

掃描的既有考卷與 Claude 生成的圖，在字體、粗細、底色、留白上會有明顯差異，混在同一份考卷中會顯得突兀。

這不是技術問題，而是「要不要在意」的問題。若在意，需統一其中一邊的產出風格。**尚未決定。**

### 螢幕呈現與列印

兩者不必求一致。螢幕適合一次一題、往下捲動；列印若有需要，可另作連續排版。**目前不實作列印專用排版。**

### 著作權

掃描的既有考卷為他人著作，公開發布於網際網路與存放於本機性質不同。此事已知，選擇不處理。

---

## 13. 修訂紀錄

### 設計過程中修正掉的錯誤判斷

**「題目應以結構化文字呈現」** — 曾建議使用 HTML `ruby`、KaTeX、SVG 以保留編輯彈性。但使用者的實際需求是「組裝已定稿的題目」，而非「編輯題目內容」。圖像化雖失去彈性，但將所有排版問題擋在系統之外，與本專案的核心訴求一致。

**「統一圖片寬度即可解決大小不一」** — 錯誤。圖框一致不等於內容字級一致。需要逐題人工校正（見第 6 節）。

**「reader 應實作全域縮放並記住設定」** — 多餘。瀏覽器原生功能已完整覆蓋此需求，包括記住各站台的縮放比例。自行實作只是重新發明一個較差的版本。

**「index 只是一串連結即可」** — 考卷累積後將難以尋找。index 需分類，且分類資訊需持久保存（見第 8、9 節）。
